# Delivery log

- 2026-07-06 — Скаффолд KMP/Compose + просмотр md (`scaffold-md-viewer`): код готов и зелёный на ветке, MR пропущен (git remote не настроен, flow локальный).
- 2026-07-06 — Стратегия локального git-хранения (`git-storage-strategy`): принято решение — shallow clone (--depth=1), история только на сервере. Только дизайн-решение, кода нет.
- 2026-07-07 — Движок git-синхронизации (JGit, слайс 1) (`git-sync-engine`): код готов и зелёный на ветке (12 тестов); спайк выявил несовместимость shallow с merge → решение о хранении пересмотрено на полный клон; конфликты .md спрашивают резолвер, не-.md — сервер. MR пропущен (remote не настроен).
- 2026-07-07 — UI git-синка (кнопка/статус/диалог) (`sync-ui`): код готов и зелёный на ветке (16 тестов); мост UiConflictResolver↔Compose, .md-конфликт спрашивает выбор, конфиг репо через BuildConfig, авто-сид убран. MR пропущен (remote не настроен).
- 2026-07-07 — GitHub OAuth Device Flow (`github-oauth-device-flow`): код готов и зелёный (24 теста); Ktor device flow, EncryptedSharedPreferences, экран входа, токен из хранилища в синк, BuildConfig.SYNC_TOKEN убран. MR пропущен (GitHub, не GitLab).
- 2026-07-07 — Экран настроек репозитория (`repo-settings`): код готов и зелёный (28 тестов); RepoSettingsStore + SettingsScreen, VaultViewModel через syncConfigProvider, BuildConfig.SYNC_REMOTE_URL убран. MR пропущен (GitHub, не GitLab).
- 2026-07-08 — Автосинк на WorkManager (`autosync`): код готов и зелёный (31 тест); BackgroundSyncRunner (USE_LOCAL в фоне), SyncWorker, периодический AutoSyncScheduler (~30 мин), планирование при входе. MR пропущен (GitHub, не GitLab).
- 2026-07-08 — Редактирование md (`markdown-editing`): код готов и зелёный (33 теста); VaultRepository.writeFile, VaultViewModel.saveFile, режим правки (просмотр↔редактор) в MarkdownScreen. MR пропущен (GitHub, не GitLab).
- 2026-07-08 — Поиск по заметкам (`note-search`): код готов и зелёный (35 тестов); VaultRepository.search (имя+содержимое, без регистра), VaultViewModel.search, поле поиска в списке. MR пропущен (GitHub, не GitLab).
- 2026-07-08 — AI-режим на OpenRouter (`ai-mode`): код готов и зелёный (45 тестов); ApiKeyStore, OpenRouterClient (tool-calling), AiAgent (search/read/write с подтверждением, лимит шагов), AiViewModel, AiChatScreen + диалог подтверждения, ключ в настройках. MR пропущен (GitHub, не GitLab).
- 2026-07-09 — AppBar + экран настроек (`appbar-settings-screen`): код готов и зелёный (46 тестов); общий `Scaffold`+`TopAppBar` в App.kt (заголовок по экрану, «Назад» и действия AI/⚙ централизованы), кустарные кнопки «Назад» убраны из экранов; SettingsScreen переписан — на каждое поле label/placeholder(пример)/supportingText(описание), единая кнопка «Сохранить» + подтверждение, показ/скрытие ключа. Заведён Compose-UI тест-харнес (Robolectric + `compose.uiTest`, debug-манифест с ComponentActivity) + сид-тест SettingsScreenTest (48 тестов всего). MR пропущен (GitHub, не GitLab).
- 2026-07-09 — Иконки/edge-to-edge + Sync в настройках + перевод UI на EN (`appbar-polish-en`): настоящие Material-иконки в AppBar (ArrowBack/Settings + contentDescription) вместо глифов, `enableEdgeToEdge()` (TopAppBar под status bar, без серой полосы); кнопка синхронизации перенесена с главного экрана в раздел «Synchronization» на экране настроек (кнопка + статус, блокировка во время sync); весь пользовательский интерфейс и AI-строки переведены на английский (49 тестов, +тест syncButtonTriggersSync). Проверено на эмуляторе. MR пропущен (GitHub, не GitLab).
- 2026-07-09 — Локализация строк через Compose Resources (`i18n-strings`): все UI-строки вынесены в `composeResources/values/strings.xml` и читаются через `stringResource(Res.string.*)` во всех экранах (App/Settings/VaultList/Markdown/AiChat/Login/ConflictDialog); формат как у Android strings.xml, готов к локалям (`values-ru/` и т.п.) с переключением по языку системы. В тесте выставлен Android-context для CMP-ресурсов под Robolectric (рефлексия). Нелокализованными оставлены неинтерфейсные строки (описания инструментов для LLM, внутренние результаты AI-агента, технические reason синхронизации). 49 тестов, проверено на эмуляторе. MR пропущен (GitHub, не GitLab).
- 2026-07-09 — Управление AI через настройки (`ai-toggle-settings`): AI по умолчанию выключен; в настройках переключатель «Enable AI», при включении раскрывается секция с полем ключа OpenRouter; кнопка «AI» в AppBar показывается только когда AI включён; флаг персистится (RepoSettingsStore.isAiEnabled/setAiEnabled, SharedPrefs), aiVm создаётся только при включённом AI. TDD: контракт стора + SettingsViewModel + 2 UI-теста (скрытие секции ключа, срабатывание переключателя). 55 тестов зелёные. MR пропущен (GitHub, не GitLab).
- 2026-07-09 — Фикс: смена Repository URL не подгружала новый репозиторий (`fix-repo-url-switch`): JGitSync клонировал только при отсутствии `.git`, поэтому при смене URL синкал старый origin; теперь при несовпадении origin (сравнение через URIish, устойчиво к нормализации) локальный клон сносится и репозиторий клонируется заново. TDD: тест `changing_remote_url_reclones_new_repo` на реальных bare-репо. 56 тестов зелёные. MR пропущен (GitHub, не GitLab).
- 2026-07-09 — Список: папки + иконки + скрытие поиска по скроллу (`list-folders-icons-scroll-search`): VaultRepository.listEntries (папки + .md, папки первыми) + rootPath/parentOf/isRoot; навигация openFolder/upFolder, currentDir/atRoot в VaultState; VaultListScreen на ListItem с иконками Folder/Description (у .md скрыт суффикс), nestedScroll прячет поле поиска синхронно со скроллом (offset панели + top-padding списка); в подпапке «Назад» = вверх по дереву, заголовок = имя папки. TDD: 3 новых теста (сортировка listEntries, навигация в подпапку, VM open/up). Все тесты зелёные. MR пропущен (GitHub, не GitLab).
- 2026-07-09 — Кликабельные [[wikilinks]] + эмбед картинок в просмотре md (`md-wikilinks`): resolveWikiLink + renderNote (чистая логика, TDD) — `[[file]]`/`[[path/file]]`/`[[file.md]]` резолвятся против vault (имя в любой папке, приоритет корня, либо точный путь), ненайденное остаётся текстом; VaultRepository.allFiles() рекурсивно + readBytes; VaultViewModel.openPath/bytesOf, allFiles в VaultState; MarkdownScreen — блоки Text/Image, ссылки перехватываются через LocalUriHandler (схема wikilink:) → навигация, `![[img.ext]]` рисуется картинкой через expect/actual decodeImage (BitmapFactory), без новых зависимостей. TDD: 9 тестов резолвера/renderNote + allFiles + openPath. Проверено на эмуляторе (все 5 приёмочных кейсов). MR пропущен (GitHub, не GitLab).
