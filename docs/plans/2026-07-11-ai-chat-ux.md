# Plan — UX-friendly UI для AI-чата (TDD)

Тесты — Robolectric + `runComposeUiTest` (см. `SettingsScreenTest`). Один тест-класс
`AiChatScreenTest`, семантика через `testTag` / `contentDescription` / текст.

1. **Строки**: добавить `chat_empty_title/subtitle`, `chat_input_hint`, `cd_ai_avatar`, `cd_send`;
   удалить `chat_you_prefix`/`chat_ai_prefix`.
2. **RED**: `AiChatScreenTest` —
   - пустой экран показывает приветствие (`chat_empty_title`);
   - при `Thinking` виден `testTag("typing")`, при `Idle` — нет;
   - кнопка `cd_send` disabled при пустом вводе и при `Thinking`, enabled при тексте;
   - ввод + клик по `cd_send` вызывает `onSend` и очищает поле;
   - аватар (`cd_ai_avatar`) есть у сообщения ассистента и нет у пользовательского;
   - `pendingWrite` показывает диалог с именем файла.
3. **GREEN**: переписать `AiChatScreen` — `MessageBubble`, `AvatarBadge`, `TypingIndicator`
   (анимация через `rememberInfiniteTransition`), `ChatInputBar`, `EmptyState`, автопрокрутка
   (`LaunchedEffect` на размер списка + статус → `animateScrollToItem`). Диалог записи сохранить.
4. **App.kt**: иконка AI-пункта → `Icons.Filled.AutoAwesome`.
5. **REFACTOR** + весь набор тестов зелёный (`:composeApp:testDebugUnitTest`).
