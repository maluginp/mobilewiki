# Autosync (WorkManager) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `aiteam-delivery-developing` to implement this plan task-by-task, strictly test-first. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Периодический фоновый синк vault через WorkManager поверх готового движка, с политикой «локальная побеждает» для `.md`-конфликтов.

**Architecture:** `BackgroundSyncRunner` (commonMain, тестируемое ядро) запускает `GitSync` с резолвером `USE_LOCAL`. `SyncWorker` (CoroutineWorker) — тонкая обёртка, собирающая зависимости из Context. `AutoSyncScheduler` планирует периодическую задачу; `MainActivity` вызывает её при входе.

**Tech Stack:** androidx.work 2.9.1, kotlinx-coroutines, существующие GitSync/stores.

## Global Constraints

- Пакеты: `app.obsidianmd.sync` (ядро), воркер/планировщик — androidMain.
- Фон: `.md`-конфликт → `USE_LOCAL`; не-`.md` — как в движке (сервер).
- Периодичность ~30 мин, `NetworkType.CONNECTED`, уникальная задача (`KEEP`).
- URL не задан → runner отдаёт `Failed`, воркер не падает.
- Аналитика не вводится (нет стека).

---

### Task 1: BackgroundSyncRunner (ядро)

**Files:**
- Create: `composeApp/src/commonMain/kotlin/app/obsidianmd/sync/BackgroundSyncRunner.kt`
- Test: `composeApp/src/commonTest/kotlin/app/obsidianmd/sync/BackgroundSyncRunnerTest.kt`

**Interfaces:**
- Consumes: `GitSync`, `SyncConfig`, `SyncResult`, `ConflictResolver`, `Resolution`, `MdConflict`.
- Produces: `class BackgroundSyncRunner(gitSync, syncConfigProvider)` c `suspend fun run(): SyncResult`.

- [ ] **Step 1: Падающие тесты**

`composeApp/src/commonTest/kotlin/app/obsidianmd/sync/BackgroundSyncRunnerTest.kt`:
```kotlin
package app.obsidianmd.sync

import kotlinx.coroutines.test.runTest
import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue

private fun cfg() = SyncConfig(remoteUrl = "file:///x", localPath = "/l", token = null)

private class RecordingGitSync(val result: SyncResult) : GitSync {
    var called = false
    override suspend fun sync(config: SyncConfig, resolver: ConflictResolver): SyncResult {
        called = true
        return result
    }
}

class BackgroundSyncRunnerTest {
    @Test
    fun no_config_fails_without_calling_engine() = runTest {
        val engine = RecordingGitSync(SyncResult.Cloned)
        val runner = BackgroundSyncRunner(engine) { null }
        val result = runner.run()
        assertTrue(!engine.called)
        assertTrue(result is SyncResult.Failed)
    }

    @Test
    fun with_config_runs_engine() = runTest {
        val engine = RecordingGitSync(SyncResult.Synced(pushed = true, conflictsResolved = 0))
        val runner = BackgroundSyncRunner(engine) { cfg() }
        val result = runner.run()
        assertTrue(engine.called)
        assertEquals(SyncResult.Synced(true, 0), result)
    }

    @Test
    fun md_conflict_resolved_use_local() = runTest {
        // движок кодирует выбор резолвера в результат
        val engine = object : GitSync {
            override suspend fun sync(config: SyncConfig, resolver: ConflictResolver): SyncResult {
                val r = resolver.resolve(MdConflict("n.md", "L", "S"))
                return SyncResult.Synced(pushed = true, conflictsResolved = if (r == Resolution.USE_LOCAL) 1 else 0)
            }
        }
        val runner = BackgroundSyncRunner(engine) { cfg() }
        assertEquals(SyncResult.Synced(true, 1), runner.run())
    }
}
```

- [ ] **Step 2: Прогнать — RED**

Run: `./gradlew :composeApp:testDebugUnitTest --tests "app.obsidianmd.sync.BackgroundSyncRunnerTest"`
Expected: FAIL — `BackgroundSyncRunner` нет (ошибка компиляции).

- [ ] **Step 3: Реализация**

`composeApp/src/commonMain/kotlin/app/obsidianmd/sync/BackgroundSyncRunner.kt`:
```kotlin
package app.obsidianmd.sync

class BackgroundSyncRunner(
    private val gitSync: GitSync,
    private val syncConfigProvider: () -> SyncConfig?,
) {
    private val localWins = ConflictResolver { Resolution.USE_LOCAL }

    suspend fun run(): SyncResult {
        val cfg = syncConfigProvider() ?: return SyncResult.Failed("репозиторий не настроен")
        return gitSync.sync(cfg, localWins)
    }
}
```

- [ ] **Step 4: Прогнать — GREEN**

Run: `./gradlew :composeApp:testDebugUnitTest --tests "app.obsidianmd.sync.BackgroundSyncRunnerTest"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add composeApp/src/commonMain/kotlin/app/obsidianmd/sync/BackgroundSyncRunner.kt composeApp/src/commonTest/kotlin/app/obsidianmd/sync/BackgroundSyncRunnerTest.kt
git commit -m "feat: BackgroundSyncRunner (local-wins background sync core) + tests"
```

---

### Task 2: Android — WorkManager, воркер, планировщик, проводка

Android-интеграция — ручная приёмка (юнит-тестов нет).

**Files:**
- Modify: `gradle/libs.versions.toml` (work версия + библиотека)
- Modify: `composeApp/build.gradle.kts` (work в androidMain)
- Create: `composeApp/src/androidMain/kotlin/app/obsidianmd/sync/SyncWorker.kt`
- Create: `composeApp/src/androidMain/kotlin/app/obsidianmd/sync/AutoSyncScheduler.kt`
- Modify: `composeApp/src/androidMain/kotlin/app/obsidianmd/MainActivity.kt`

**Interfaces:**
- Consumes: `BackgroundSyncRunner`, `JGitSync`, `SyncConfig`, `EncryptedTokenStore`, `SharedPrefsRepoSettingsStore`, `vaultRoot`.

- [ ] **Step 1: Зависимость WorkManager**

`gradle/libs.versions.toml` — в `[versions]`: `work = "2.9.1"`; в `[libraries]`:
```toml
androidx-work-runtime = { module = "androidx.work:work-runtime-ktx", version.ref = "work" }
```
`composeApp/build.gradle.kts` — в `androidMain.dependencies`:
```kotlin
            implementation(libs.androidx.work.runtime)
```

- [ ] **Step 2: SyncWorker.kt**

```kotlin
package app.obsidianmd.sync

import android.content.Context
import androidx.work.CoroutineWorker
import androidx.work.WorkerParameters
import app.obsidianmd.auth.EncryptedTokenStore
import app.obsidianmd.settings.SharedPrefsRepoSettingsStore
import app.obsidianmd.vaultRoot

class SyncWorker(context: Context, params: WorkerParameters) : CoroutineWorker(context, params) {
    override suspend fun doWork(): Result {
        val ctx = applicationContext
        val tokenStore = EncryptedTokenStore(ctx)
        val settingsStore = SharedPrefsRepoSettingsStore(ctx)
        val vaultPath = vaultRoot(ctx).toString()
        val runner = BackgroundSyncRunner(JGitSync()) {
            settingsStore.getRemoteUrl()?.takeIf { it.isNotBlank() }?.let { url ->
                SyncConfig(remoteUrl = url, localPath = vaultPath, token = tokenStore.get())
            }
        }
        return when (runner.run()) {
            is SyncResult.Failed -> Result.retry()
            else -> Result.success()
        }
    }
}
```

- [ ] **Step 3: AutoSyncScheduler.kt**

```kotlin
package app.obsidianmd.sync

import android.content.Context
import androidx.work.Constraints
import androidx.work.ExistingPeriodicWorkPolicy
import androidx.work.NetworkType
import androidx.work.PeriodicWorkRequestBuilder
import androidx.work.WorkManager
import java.util.concurrent.TimeUnit

class AutoSyncScheduler(private val context: Context) {
    fun schedule() {
        val request = PeriodicWorkRequestBuilder<SyncWorker>(30, TimeUnit.MINUTES)
            .setConstraints(
                Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build(),
            )
            .build()
        WorkManager.getInstance(context).enqueueUniquePeriodicWork(
            "obsidian-autosync",
            ExistingPeriodicWorkPolicy.KEEP,
            request,
        )
    }

    fun cancel() {
        WorkManager.getInstance(context).cancelUniqueWork("obsidian-autosync")
    }
}
```

- [ ] **Step 4: MainActivity — планировать при входе**

В `MainActivity.onCreate`, после определения `loggedIn`/входа: когда пользователь залогинен,
запланировать автосинк один раз. Проще всего — в `setContent` при `loggedIn == true` вызвать
через `LaunchedEffect`:
```kotlin
            if (loggedIn) {
                androidx.compose.runtime.LaunchedEffect(Unit) {
                    app.obsidianmd.sync.AutoSyncScheduler(applicationContext).schedule()
                }
            }
```
(вставить внутри `setContent { ... }` до/рядом с ветвлением `if (!loggedIn) ... else ...`.)

- [ ] **Step 5: Собрать**

Run: `./gradlew :composeApp:assembleDebug`
Expected: `BUILD SUCCESSFUL`.

- [ ] **Step 6: Ручная приёмка**

Прогнать кейсы из спеки: фон обновляет vault; конфликт → локальная версия; без URL — воркер не
падает. (Принудительный запуск задачи — через Android Studio App Inspection → WorkManager, или
`adb shell cmd jobscheduler run`.)

- [ ] **Step 7: Commit**

```bash
git add gradle/libs.versions.toml composeApp/build.gradle.kts composeApp/src/androidMain
git commit -m "feat: WorkManager autosync (SyncWorker + periodic scheduler + wiring)"
```

---

## Self-review

**Покрытие спеки:**
- `BackgroundSyncRunner` (ядро, USE_LOCAL, гард на конфиг) → Task 1.
- `SyncWorker` → Task 2 Step 2; `AutoSyncScheduler` (периодика, сеть, KEEP) → Step 3;
  планирование при входе → Step 4; зависимость WorkManager → Step 1.
- Приёмочные кейсы → в спеке; Task 2 Step 6.
- Аналитика → неприменима.

**Placeholder-скан:** реальный код/команды; заглушек нет.

**Согласованность типов:** `BackgroundSyncRunner(gitSync: GitSync, syncConfigProvider: () -> SyncConfig?)`
c `run(): SyncResult`; `SyncWorker : CoroutineWorker`; `AutoSyncScheduler(context){schedule(),cancel()}`;
уникальное имя задачи `"obsidian-autosync"`. Используемые `SyncConfig`/`SyncResult`/`GitSync`/
`Resolution`/`MdConflict`/`JGitSync`/`EncryptedTokenStore`/`SharedPrefsRepoSettingsStore`/`vaultRoot`
— существующие, имена совпадают.
