<intent>
Улучшить экран списка заметок: показывать папки vault (сейчас видны только .md
из корня), навести красоту (иконки в строках списка), и прятать поле поиска
при скролле вниз, показывая его при скролле вверх — плавно, синхронно со скроллом.
</intent>

<context>
Проект: KMP/Compose просмотрщик Obsidian-vault. Экран списка — VaultListScreen.
Репозиторий чтения — VaultRepository, состояние — VaultViewModel.
Tracker: none (Jira не используется), flow: mr (remote не настроен → MR пропускается).

Проблема сейчас:
- VaultRepository.listMarkdownFiles() делает fs.list(root) и оставляет только
  обычные .md — папки и вложенные файлы не отображаются вообще.
- Строки списка — простой Text без иконок.
- Поле поиска (OutlinedTextField) статично сверху, не реагирует на скролл.
</context>

<requirements>
- Папки vault отображаются в списке; тап по папке открывает её содержимое,
  «Назад» поднимается на уровень выше (в подпапке).
- Строки списка с ведущей иконкой (папка / документ), опрятный вид (ListItem),
  у .md-файлов расширение в заголовке скрыто.
- Поле поиска скрывается при скролле вниз и появляется при скролле вверх,
  движение синхронно со скроллом и плавно.
</requirements>
