<intent>
Переделать UX выбора AI-модели: в настройках — текущая модель + иконка «редактировать»;
по клику открывается отдельный экран со списком доступных моделей.
</intent>

<requirements>
- В настройках вместо поля-автокомплита показывать текущую модель и иконку редактирования.
- По клику на иконку открывается экран со списком доступных моделей.
- В AppBar экрана списка доступна иконка «Поиск» (фильтр по списку).
- При загрузке списка показывать индикатор загрузки (вертелку).
- Pull-to-refresh на списке моделей.
- В строке модели показывать полезные данные (не только id): имя, id, размер контекста, цена.
</requirements>

<context>
Источник: устный запрос (tracker: none). Ветка: model-picker-ux.

Текущее состояние: выбор модели — inline `ExposedDropdownMenu` (автокомплит) в
SettingsScreen. Список моделей грузится в SettingsViewModel (models/modelsLoading),
данные модели — только id + name. Навигация в приложении — булевы флаги в App.kt
(showSettings/showAi/homeSearching), общий Scaffold+TopAppBar централизованы там же.
OpenRouter `/models` отдаёт id, name, context_length, pricing{prompt,completion}.
</context>
